# MM Scribe — 封包偵測技術筆記

> 給未來的自己 / 未來的 Claude 對話用：快速掌握目前封包解析邏輯與已鎖定的旗標位置。
> 對應原始碼版本：`LiveDamageMonitor_v0.10.py`、`PacketParser.py`

---

## 1. 資料來源

- **來源**：透過 `scapy.sniff()` 攔截 TCP 封包（BPF 過濾器 `ip net 43.0.0.0/8 and tcp`）
- **需求**：Windows 需安裝 [Npcap](https://npcap.com/) 驅動；程式啟動時會偵測並在底部狀態列顯示
- **權限**：抓封包需要系統管理員身分執行

---

## 2. 封包整體結構

每個 TCP payload 內含多個 TLV (Type-Length-Value) 段落：

```
[cmd 4 bytes] [size 4 bytes little-endian] [payload ... size bytes]
[cmd 4 bytes] [size 4 bytes little-endian] [payload ... size bytes]
...
```

程式一段一段掃過去，找 `cmd == 0xE9 0x51 0x00 0x00`（即 **0x51E9 傷害事件**）時展開解析。

---

## 3. 0x51E9 傷害事件內部佈局

從事件 header 起算的相對 offset：

| Offset | 長度 | 內容 |
|--------|------|------|
| 0..3 | 4 | cmd = `e9 51 00 00` |
| 4..7 | 4 | size (通常 = `35 00 00 00` = 53) |
| 8 | 1 | 未知 byte，用途不明 |
| 9..16 | 8 | **攻擊者 ID** (uint64 LE) |
| 17..24 | 8 | **目標 ID** (uint64 LE) |
| 25..28 | 4 | **傷害數值** (uint32 LE) |
| 29..60 | 32 | **附加資訊 tail**（標籤旗標藏在這裡） |

整個事件總長 = 4 + 4 + 53 = **61 bytes**

### 特殊值
- `dmg_val == 0xFFFFFFFF` → 傷害免疫（不計入統計，只印訊息）

---

## 4. 標籤旗標位置

三個關鍵 byte（相對 0x51E9 header 起算）：

| 變數名 | 絕對 offset | 相當於 tail 位置 |
|--------|------------|-----------------|
| `b41` | `offset+41` | tail[12] |
| `b42` | `offset+42` | tail[13] |
| `b57` | `offset+57` | tail[28] |

### `b41` bit 對應（已知遮罩 `0xCD`）

| bit | mask | 標籤 | 備註 |
|-----|------|------|------|
| 0 | `0x01` | **爆擊** | 100% 確認 |
| 2 | `0x04` | **無防備** | 需**額外檢查 bit 3=0**，否則會被誤判為破防（兩者共用此 bit） |
| 3 | `0x08` | **破防** | 100% 確認 |
| 6 | `0x40` | *（非標籤）* | 用途不明，普攻與部分技能會亮。**不當 tag 顯示**，加進 known mask 忽略。2026-08 樣本觀察到 |
| 7 | `0x80` | *（非標籤）* | **普通攻擊指示**：玩家自動攻擊=1；玩家技能 or 寵物攻擊=0。**不當 tag 顯示**，僅用於分類 |

### `b42` bit 對應（已知遮罩 `0x07`）

| bit | mask | 標籤 | 備註 |
|-----|------|------|------|
| 0 | `0x01` | **多重打擊** | 寵物多段攻擊測試確認 |
| 1 | `0x02` | **強擊** | 100% 確認 |
| 2 | `0x04` | **連擊** | 100% 確認 |

### `b57` bit 對應（已知遮罩 `0x01`）

| bit | mask | 標籤 | 備註 |
|-----|------|------|------|
| 0 | `0x01` | **破防** | 與 `b41` bit 3 完全同步的**備援旗標**（兩者只要一個亮就算破防） |

---

## 5. 技能識別（Skill ID）★ 2026-08 新發現 ★

**每筆傷害的來源技能可以識別**。0x51E9 事件本身不含技能 ID（tail 32 byte 除了旗標位以外全為 0），但**緊接在 0x51E9 後面**的另一個 TLV `0x00004fc5` 內含 skill ID。

### `0x00004fc5` TLV 結構（size 35 bytes）

```
[cmd 4B: c5 4f 00 00] [size 4B: 23 00 00 00 = 35] [payload 35B]
```

payload 內部佈局：

| Offset (payload) | 長度 | 內容 |
|-----------------|------|------|
| 0 | 1 | tag `0x00` |
| 1..8 | 8 | 攻擊者 ID (uint64 LE) |
| 9..16 | 8 | 目標 ID (uint64 LE) |
| **17..20** | **4** | **★ Skill ID (uint32 LE) ★** |
| 21..24 | 4 | 保留 `00 00 00 00` |
| 25 | 1 | b41 echo（與 0x51E9 tail[12] 相同） |
| 26 | 1 | b42 echo（與 0x51E9 tail[13] 相同） |
| 27..32 | 6 | 保留 `00` |
| 33 | 1 | b57 echo |
| 34 | 1 | 通常 `0x01` |

### 解析建議

原本 parser 掃到 `e9 51 00 00` 展開 0x51E9 後，**再往後掃到下一個 `c5 4f 00 00`**（通常就在幾個 byte 之後），讀 payload offset 17..20 就是這筆傷害的技能 ID。attacker/target 兩邊都 match，配對零歧義。

### 已知技能 ID 對照表（戰士，2026-08 樣本）

| 技能 | Skill ID (uint32 LE) | 備註 |
|------|----------------------|------|
| 普攻 | `0x64d5b11d` | 3 樣本一致 |
| 技能1（base） | `0x21dd59c4` | 3 樣本一致 |
| 技能1（另一變體） | `0x54866b87` | 舊樣本推測為 技能1+，待確認 |
| 技能2（base） | `0x47903d5c` | 3 樣本一致 |
| 技能2（另一變體） | `0x0208bb05` | 舊樣本，pet-based 版，推測為 技能2+ |
| 技能4（base） | `0x6aff74ea` | 3 樣本一致 |
| **技能4+** | `0x563674cc` | **3 樣本一致；與 base 是完全不同的 ID** |
| 技能5 | `0x4018e42a` | 舊樣本，多目標多段命中共用同一 ID |

### 關鍵性質

- **+ 變體是獨立 skill ID**：技能4 (`0x6aff74ea`) vs 技能4+ (`0x563674cc`) 完全不同。伺服器把 + 變體當獨立技能傳，不是共用 ID 加旗標。
- **同技能跨施放 100% 一致**：不論爆擊、破防、命中不同目標，同一個技能的 ID 都一樣。
- **多段命中共用同 ID**：技能5 兩段打不同目標，都是 `0x4018e42a`。
- **不是雜湊或時間戳**：值穩定，可以直接當查表 key。

### 特殊值：`0x00000000` = **疑似符文傷害**

當 skill ID = `0x00000000` 時，這筆傷害**不是玩家主動施放的技能**，來源說法（使用者確認）：
> 遊戲中很多符文有「攻擊目標 N 次後造成 1 次額外傷害」之類的機制，這類**符文附加的額外傷害**就是 skill ID = 0。

**判定建議**：monitor 顯示這類傷害時直接標為 **「疑似符文傷害」**，不要試圖查名字。

**輔助訊號**（觀察自雷符文 3 個樣本，2026-08）：

- 附近會出現 UTF-16 字串（在 `0x00005096` size 104 的 TLV 內），命名格式類似：
  - `Elemental_Common_Hit_Lightning`（雷元素）
  - `BattleMusician_StarlightSonata_Hit_1`（詩人星光奏鳴曲）
  - 未來若錄到其他元素，字尾應為 `_Fire` / `_Ice` / `_Wind` 等
- `b42` 常出現原始 mask 外的位元 `0x08` 和 `0x80`（即 `0x88`），跟已知標籤 bit 不重疊。這兩個 bit 目前推測是「元素/被動效果類型旗標」，**不當作標籤顯示**
- 傷害數值通常比同段的一般攻擊高

**未驗證的點**（樣本不足，先假設寬鬆處理）：
- 只驗證過雷符文 1 種，**尚未確認所有符文附加傷害都用 skill ID = 0**。可能存在部分符文有自己的獨立 skill ID
- 若之後遇到「skill ID 非 0 但顯示行為像符文」的樣本，需要重新分類

### 樣本來源

`E:\瑪奇封包\標籤測試封包\戰士技能標籤測試\` 有 15 筆封包（普攻 × 3、技能1 × 3、技能2 × 3、技能4 × 3、技能4+ × 3），每一組 3 筆的 skill ID 完全一致，跨組不同，交叉驗證通過。

---

## 6. 尚未鎖定的標籤

| 標籤 | 現況 |
|------|------|
| **追擊** | 測試資料完全沒出現過，bit 位置未知 |
| 其他 | 若 `b41`/`b42` 出現已知遮罩以外的 bit → 日誌顯示 `未知(b41.XX,b42.XX)` 供人工比對 |

---

## 7. 標籤判定虛擬碼

```python
KNOWN_MASK_B41 = 0xCD    # bit 0 + bit 2 + bit 3 + bit 6 + bit 7
KNOWN_MASK_B42 = 0x07    # bit 0 + bit 1 + bit 2

tags = []
if b41 & 0x01:                              tags.append("爆擊")
if b42 & 0x02:                              tags.append("強擊")
if (b41 & 0x08) or (b57 & 0x01):            tags.append("破防")
if (b41 & 0x04) and not (b41 & 0x08):       tags.append("無防備")
if b42 & 0x04:                              tags.append("連擊")
if b42 & 0x01:                              tags.append("多重打擊")

# 檢查未知位元
unknown_b41 = b41 & ~KNOWN_MASK_B41 & 0xFF
unknown_b42 = b42 & ~KNOWN_MASK_B42 & 0xFF
if unknown_b41 or unknown_b42:
    tags.append(f"未知(b41.{unknown_b41:02X},b42.{unknown_b42:02X})")

# 全空 → 「普通」
```

---

## 8. 統計功能

### DPS（每秒傷害）
- 公式：`total_damage / max(last_damage_time - first_damage_time, 1.0)`
- 停手不打時 DPS 會**凍結**（不會因閒置而遞減）

### 覆蓋率
- 樣本門檻：`COVERAGE_MIN_HITS = 10`（未達門檻顯示 `—`）
- 公式：`(該標籤出現次數 / 總傷害筆數) * 100`
- 目前顯示：**爆擊 / 強擊 / 連擊** 三項

---

## 9. 驗證資料集

- `E:\瑪奇封包\標籤測試封包\戰士\`：**19 筆戰士職業封包 hex dump**，對應 `標籤統計-戰士.csv`。用於確認旗標位元。
- `E:\瑪奇封包\標籤測試封包\戰士技能標籤測試\`：**15 筆技能識別封包**（普攻/技能1/技能2/技能4/技能4+ 各 3 筆）。用於確認 `0x00004fc5` skill ID 欄位。
- `E:\瑪奇封包\標籤測試封包\帶攻擊的符文\`：**3 筆符文攻擊封包**（雷符文樣本）。用於觀察 skill ID = 0 的符文附加傷害行為。

### 已確認的 5 個標籤 + 2 個屬性 + 1 個技能識別
- 標籤：爆擊、強擊、連擊、破防、無防備、多重打擊
- 屬性：b41.bit7（普通攻擊指示）、b41.bit6（用途不明，需忽略）
- 技能識別：`0x00004fc5` payload offset 17..20 = uint32 skill ID
- 特殊值：skill ID = `0x00000000` → 疑似符文傷害

---

## 10. 開發者模式 log 格式

程式勾選「🛠 開發者」後底部會出現診斷面板，每筆傷害輸出：

```
[Flag] 數值: 12345 | b41:81 b42:02 b57:00
```

方便手動核對某個 bit 是否符合預期標籤。

---

## 11. 相關檔案

| 檔案 | 用途 |
|------|------|
| `LiveDamageMonitor_v0.10.py` | 即時監控主程式（customtkinter GUI） |
| `PacketParser.py` | Hex dump txt 檔的靜態解析工具 |
| `build_all.bat` | 一次打包開發版 + 發布版 EXE |
| `rebuild_icon.py` | 把單尺寸 ICO 補成多尺寸 |
| `refit_icon.py` | 自動裁切透明邊、置中放大 icon |
| `標籤測試封包/戰士/` | 19 筆旗標驗證封包 + CSV 對照表 |
| `標籤測試封包/戰士技能標籤測試/` | 15 筆技能識別封包（每技能 3 筆） |
| `標籤測試封包/帶攻擊的符文/` | 3 筆雷符文樣本，觀察 skill ID = 0 行為 |

---

## 12. 未來可補強方向

- [ ] 補充 **追擊** 標籤的測試封包，鎖定其 bit 位置
- [ ] 探索寵物 vs 玩家的可靠區分方式（目前只能靠 attacker ID）
- [ ] 探索 `b41` bit 1、bit 4、bit 5、bit 6 的意義（bit 6 已觀察到但用途未知）
- [ ] 覆蓋率加入 破防 / 無防備 / 多重打擊
- [ ] **在 parser / monitor 加入 `0x00004fc5` 解析，抽出 skill ID**
- [ ] **建立各職業「skill_id → skill_name」對照表**（每技能 1-3 樣本即可，含 + 變體分開錄）
- [ ] 探索 `0x00004fc5` payload offset 25-26 的 b41/b42 echo 是否與 0x51E9 完全同步（初步觀察是）
- [ ] **驗證 skill ID = 0 是否涵蓋所有符文附加傷害**（目前只錄過雷符文，需要不同機制的符文交叉驗證）
- [ ] 解讀 `b42` 的 bit 3 (`0x08`) 和 bit 7 (`0x80`)，推測是符文/元素效果的類型旗標
- [ ] 從 `0x00005096` TLV 內的 UTF-16 字串（`Elemental_*` / `Musician_*`）推導符文效果的可讀名稱
